const FILTER_TYPE = Object.freeze({
    ALL: 'all',
    ACTIVE: 'active',
    COMPLETED: 'completed'
});

const TODO_STATUS = Object.freeze({
    UPDATABLE: 'updatable',
    EDITING: 'editing',
    COMPLETED: 'completed'
})

export { FILTER_TYPE, TODO_STATUS }